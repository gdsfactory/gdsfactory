[tool.poetry]
name = "gdsfactory"
version = "1.3.3"
description = "build PDKs and GDS layouts"
authors = [""]
license = "MIT"

[tool.poetry.dependencies]
phidl = "^1.3.0"
omegaconf = "^2.0.1"
click = "^7.1.2"
jsondiff = "^1.2.0"
networkx = "^2.5"
pandas = "^1.1.2"
klayout = "^0.26.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
