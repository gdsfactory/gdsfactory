#!/usr/bin/env python
from tqdm import tqdm


py.test --monkeytype-output=./monkeytype.sqlite3

for m in tqdm($(monkeytype list-modules).split('\n')):
    if m.startswith('pp'):
        print(m)
        monkeytype stub @(m)
        monkeytype apply @(m)
